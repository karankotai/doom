/**
 * API route handler placeholder.
 *
 * Responsibilities:
 * - Handle API requests that need server-side processing
 * - Most API calls should go directly to the backend service
 * - Use this for Next.js-specific server functionality only
 *
 * Prefer calling the backend directly from client components.
 */

import { NextResponse } from "next/server";

export async function GET() {
  // TODO: Implement if needed
  return NextResponse.json({ status: "ok" });
}
